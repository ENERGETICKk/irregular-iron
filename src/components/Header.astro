---
import astroLogo from '../assets/logo.svg';
import background from '../assets/fondo.svg';
import '../styles/global.css';

---



<div class="container">
	<img src={background.src} alt="Background" class="background" />
    <header class="header">
        <div class="logo">
            <a href="/user">
                <img src={astroLogo.src} alt="Astro Logo" width="50"/>
            </a>
        </div>
        <div class="links">
            <a href="/">Home</a>
            <a href="">🔍</a>
            <a href="">⚙️</a>
            <a href="">🏫</a>
        </div>
    </header>
</div>

<style>
    /* Header futurista / tecnológico */
.header {
  position: sticky;
  top: 0;
  left: 0;
  width: 100%;
  height: 120px;
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 1.5rem 3rem;
  border-radius: 1rem;
  background: rgba(0, 26, 77, 0.6); /* azul oscuro semi-transparente */
  backdrop-filter: blur(10px);
  box-shadow: 0 0 20px rgba(0, 153, 255, 0.5), 0 0 40px rgba(0, 204, 255, 0.3);
  border: 1px solid rgba(0, 204, 255, 0.4);
  z-index: 1000;
  transition: background 0.3s ease, box-shadow 0.3s ease;
}

/* Logo con efecto glow */
.logo img {
  width: 80px;
  border-radius: 50%;
  transition: transform 0.3s ease, box-shadow 0.3s ease;
  box-shadow: 0 0 10px #00ccff, 0 0 20px #3399ff;
}

.logo img:hover {
  transform: scale(1.2);
  box-shadow: 0 0 20px #00e5ff, 0 0 40px #3399ff;
}

/* Links estilo futurista */
.links {
  display: flex;
  gap: 3rem;
  margin-right: 3rem;
}

.links a {
  text-decoration: none;
  color: #66ccff;
  font-weight: 600;
  font-size: 1.8rem;
  transition: transform 0.25s ease, text-shadow 0.25s ease, color 0.25s ease;
  text-shadow: 0 0 5px #00ccff, 0 0 15px #3399ff;
}

.links a:hover {
  transform: scale(1.3);
  color: #00ffff;
  text-shadow: 0 0 15px #00ccff, 0 0 35px #66ffff;
}

/* Fondo del header fijo y animación ligera */
.background {
  position: fixed;
  inset: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  z-index: -1;
  pointer-events: none;
  filter: brightness(0.7) contrast(1.2);
  transition: filter 0.3s ease;
}

/* Responsive header */
@media (max-width: 768px) {
  .header {
    flex-direction: column;
    height: auto;
    padding: 1rem 1.5rem;
  }
  .links {
    gap: 1.5rem;
    margin: 1rem 0 0 0;
  }
}

</style>

<script>
    // optional chaining — no error if null
    document.querySelector('.acceder')?.addEventListener('click', () => {
        window.location.href = '/index';
    });
</script>